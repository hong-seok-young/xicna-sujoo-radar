# 수주레이더 인수인계 메모 — 2026-05-22

다른 계정에서 이어서 작업할 때 참고용. 짧게 정리.

---

## 1. 프로젝트 개요
- **수주레이더 (Sujoo Radar)** — 자이씨앤에이 영업팀용 수주정보 자동수집 시스템
- 4-Stage 파이프라인: Stage 0(수집) → Stage 1(룰필터) → Stage 2(Haiku 분류) → Stage 3(Sonnet 추출) → Stage 4(리포트)
- 주기: 매주 금요일 (오늘 2026-05-22 금)
- 임계값: 450억+, 7일 윈도우, 산업 후보 동 약 1,152개 화이트리스트
- 위치: `C:\Users\Administrator\Documents\Vibecoding\sujoo_radar\sujoo_radar\`

## 2. 이번 세션에서 한 일

### (1) 코드 견고성 감사 — "머리로 고친 것" → 코드로 박기
사용자 지적: 클로드가 대화 중 머리로 해주던 것들이 자동 실행 때는 동작 안 함 → 전부 코드로 박아야 함.

수정 파일:
- **`src/stage0_collect/_eais_cost.py`**
  - `DONG_CATEGORY_MAP` 추가 — yaml 카테고리(반도체_디스플레이/이차전지/제약_바이오/연구개발/식품/일반제조) → 단가 카테고리 매핑
  - `SCHOOL_KEYWORDS` 확장 — 초·중·고·대학교, 유치원, 학원, 아카데미, 혁신파크, 혁신센터, 창업센터, 복지, 도서관, 박물관, 문화센터
  - `INDUSTRIAL_PURPOSES_FOR_DONG_SIGNAL = ("공장","창고","위험물","발전시설","자원순환","운수")` — 동 시그널은 산업 전용 용도일 때만 적용 (강남 R&D 동 안의 근생/업무 false positive 방지)
  - `_is_school()`, `_is_industrial_purpose_for_dong()` 헬퍼
  - `infer_category()` 우선순위: 학교컷 → 동 클러스터 시그널(산업용도 한정) → 키워드매칭 → "기타"
  - `estimate_cost_man()` 에 `dong_categories` 파라미터 추가

- **`src/stage0_collect/eais.py`**
  - `filter_and_convert()` 가 `dong_categories` 받도록 수정
  - 호출처 두 군데(line ~511, ~557) 에서 `industrial_dongs.csv` 의 categories 컬럼 파싱해서 전달

- **`scripts/analyze_eais_filter.py`** — 동 카테고리 로드해서 시뮬레이션에 반영

### (2) HTML 리포트 EAIS 섹션 추가
- **`scripts/daily_report_html.py`**
  - `render_eais_card()` 추가 — `data/raw/eais_{date}.jsonl` 읽어서 카드 렌더
  - `--eais` CLI 인자 추가
  - nav/통계그리드(7컬럼)/풋터에 EAIS 반영
  - EAIS 섹션은 G2B와 DART1 사이에 배치 (#eais)

### (3) 05-22 리포트 생성
- **결과물**: `data/daily_report_2026-05-22.html` 생성됨
- ⚠️ **불완전**: 실제 Stage 0 EAIS 실행 못 하고 21일 데이터 복사로 폴백, DART도 기본 경로(오늘 날짜)가 비어서 0건으로 나옴.
  - EAIS 21일 → 22일 cp 폴백: `data/raw/eais_2026-05-22.jsonl` (45건, 구포맷)
  - 출력: G2B 7, EAIS 45, DART1 0, DART2 0, RSS-HIGH 8, RSS-MID 62, RSS-LOW 20

## 3. 남은 작업 (인수 시 즉시 할 것)

### A. 실제 Stage 0 EAIS 실행
세션 중 Bash 가 "claude-opus-4-7 is temporarily unavailable" 로 자꾸 거부해서 못 돌렸음.
```powershell
python -m src.stage0_collect.eais --days 7 --quota 900
```
- API quota: 1,000/일, 마진 100 → 900 권장
- 캐시 우선순위: untouched → stale_active(7d) → stale_empty(30d)
- 결과: `data/raw/eais_2026-05-22.jsonl` 갱신

### B. HTML 재생성 (실데이터로)
```powershell
python scripts/daily_report_html.py `
  --rss data/filtered/2026-05-20.jsonl `
  --g2b data/raw/g2b_2026-05-20.jsonl `
  --dart data/raw/dart_2026-05-21.jsonl `
  --eais data/raw/eais_2026-05-22.jsonl `
  --period-days 7
```
DART 경로 명시 안 하면 오늘날짜 파일(없음) 찾아서 0건 나옴 — 반드시 21일 경로 명시.

### C. 21일 HTML은 건들지 말 것
`data/daily_report_2026-05-21.html` 은 사용자가 원본 보존 요구. 22일 버전만 새로.

## 4. 운영 한계 (아직 문서화 안 됨)
- 자치구 미구분 한계 (서울 자치구 단위 인허가 불완전)
- `build_industrial_dongs.py` 빌드 의존성 (외부 데이터 정합성)

## 5. 데이터 현황
- `data/filtered/2026-05-20.jsonl`: RSS 90건
- `data/raw/g2b_2026-05-20.jsonl`: G2B 7건
- `data/raw/dart_2026-05-21.jsonl`: DART 184건 ⚠️ 22일자 없음 → 명시 필요
- `data/raw/eais_2026-05-21.jsonl`: EAIS 45건 (구포맷, 추정공사비 미포함)
- `data/raw/eais_2026-05-22.jsonl`: 21일 cp 폴백 — **실데이터로 교체 필요**
- `data/cache/eais/`: 3개 동 캐시됨 (강남 개포동, 이천 부발읍, 아산 탕정면)

## 6. 환경 메모
- Windows 11, PowerShell, Python (sujoo_radar 폴더 기준)
- Stage 2 모델: `claude-haiku-4-5-20251001` (미구현)
- Stage 3 모델: `claude-sonnet-4-6` (미구현)
- 응답 언어: 한국어

## 7. 미해결 의문
- 사용자 질문 "왜 교육연구시설만 봐?" → 근생/공장/창고도 검토했다고 답변했으나 표면적이었음. 다른 용도들(업무시설/판매시설/숙박시설/관광휴게시설 등)에 대한 false positive 점검은 추가로 필요할 수 있음.
